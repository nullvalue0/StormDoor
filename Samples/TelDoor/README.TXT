TelDoor is a simple door used to telnet to another server. It uses the 
MS Winsock control to get a connection to another server's telnet port, 
then send and recieve data back and forth to the client.

This sample is intended for beginner to intermediate programmers.